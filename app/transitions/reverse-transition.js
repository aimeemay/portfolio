import { isAnimating, finish, timeSpent, animate, stop } from "liquid-fire";

export default function(opts) {

    // newView.$().css({
    //   display: '',
    //   visibility: ''
    // });

    var old = this.oldElement;

    old.visible

    this.oldElement.$().css({
        visibility: ''
    })


    this.newElement.$().css({
        visibility: ''
    })

    debugger;


}
